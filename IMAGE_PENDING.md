# Image Pending

Projects without matched hero images:

- **Indice KP** (`indice-kp`) — no archive match found
- **S.VISTA** (`s-vista-1`) — only flyers available, no performance photos. Using placeholder.
- **S.VISTA** (`s-vista-instant-soluble`) — only flyers available, no performance photos. Using placeholder.
- **S.VISTA** (`s-vista-forms-of-dance-1`) — only flyers available, no performance photos. Using placeholder.
- **S.VISTA** (`s-vista-forms-of-dance-2`) — only flyers available, no performance photos. Using placeholder.
